
apply plugin: 'java'
apply plugin: 'eclipse-wtp'
apply plugin: 'war'

// ��Ŭ���� WTP �÷����� ��� ������Ƽ ����
eclipse {
  wtp {
    facet {
      facet name: 'jst.web', version: '3.0'
      facet name: 'jst.java', version: '1.7'
    }
  }
}

repositories {
    mavenCentral()
}

dependencies {
	compile 'org.springframework:spring-webmvc:4.0.6.RELEASE'
	
	/*compile 'http://services.gradle.org/distributions/gradle-1.10-bin.zip'
	.오류,,.Compilation failed; see the compiler error output for details. Could not execute build using Gradle distribution 'http://services.gradle.org/distributions/gradle-1.10-bin.zip'.*/
	
    compile group: 'commons-collections', name: 'commons-collections', version: '3.2'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
/*

sourceCompatibility = 1.5
version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Gradle Quickstart', 'Implementation-Version': version
    }
}



test {
    systemProperties 'property': 'value'
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}
*/